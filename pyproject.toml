[project]
name = "investment-research-analyst-agent"
version = "0.1.0"
description = "A learning project for an investment research analyst agent."
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
